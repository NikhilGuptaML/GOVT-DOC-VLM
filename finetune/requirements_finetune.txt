# ── Finetuning dependencies (GPU workstation only) ─────────
# Install: pip install -r requirements_finetune.txt

torch>=2.1.0
transformers>=4.45.0
peft>=0.12.0
bitsandbytes>=0.43.0
accelerate>=0.33.0
datasets
pillow
tqdm
editdistance
# ── GGUF local inference (llama.cpp) ───────────────────────
# CUDA build required for GPU acceleration:
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python
llama-cpp-python>=0.3.0
